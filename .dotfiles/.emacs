;;; My emacs config file

;; Autosave not that often
(setq auto-save-interval 1000
      auto-save-timeout 600)

;; Show line and column number all the time
(line-number-mode 1)
(column-number-mode 1)